name: Node.js CI/CD

on:
  push:
    branches: [ "main" ]
    paths-ignore:
      - 'README.md'
      - '/assets/**'
  pull_request:
    branches: [ "main" ]

permissions:
  contents: write

jobs:
  build-deploy:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.19.4]

    steps:
    - uses: actions/checkout@v4

    - name: Set release metadata
      if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      run: |
        SHORT_SHA="${GITHUB_SHA::7}"
        echo "TEMPLATE_EDITOR_RELEASE_TAG=template-editor-build-${SHORT_SHA}" >> "$GITHUB_ENV"
        echo "TEMPLATE_EDITOR_ZIP_NAME=template-editor-${SHORT_SHA}.zip" >> "$GITHUB_ENV"

    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'

    - name: Install dependencies
      run: npm ci
    - name: Install template-editor dependencies
      working-directory: ./projects/template-editor
      run: npm ci

    - name: Build all projects
      run: npm run build:all:demo

    - name: Archive template-editor build
      if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      run: |
        cd dist
        zip -r "$TEMPLATE_EDITOR_ZIP_NAME" template-editor

    - name: Copy all built files to deploy folder
      run: |
        mkdir deploy
        mkdir deploy/template-editor

        cp -r dist/shell/* deploy/
        cp -r dist/template-editor/* deploy/template-editor/

    - name: Deploy to GitHub Pages
      if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      uses: peaceiris/actions-gh-pages@v4
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./deploy

    - name: Create template-editor release
      if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      uses: softprops/action-gh-release@v2
      with:
        tag_name: ${{ env.TEMPLATE_EDITOR_RELEASE_TAG }}
        name: Template Editor Build ${{ env.TEMPLATE_EDITOR_RELEASE_TAG }}
        generate_release_notes: true
        files: dist/${{ env.TEMPLATE_EDITOR_ZIP_NAME }}
